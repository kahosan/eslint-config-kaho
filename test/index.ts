const a = 'asd';

const obj = {
  s: 1,
  ds: 2
};

const v = `${a}/${obj.ds}`;

if (a) {
  console.log(' ');
} else {
  console.log(a);
}

if (a) console.log(a);

if (a) { console.log(a); }

const b = {
  a,
  sd: 2
};

export {};
